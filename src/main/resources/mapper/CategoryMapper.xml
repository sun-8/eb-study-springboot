<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.web.mapper.CategoryMapper">

    <!-- 카테고리 목록 -->
    <select id="selectList" resultType="com.study.web.model.CategoryDTO">
        SELECT
            ID,
            NAME
        FROM CATEGORY
        ORDER BY ID ASC
    </select>

</mapper>